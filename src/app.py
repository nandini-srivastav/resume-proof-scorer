"""
Streamlit entry point.

TODO: build the actual UI — JD input, resume upload(s), a results
table showing Keyword Score vs Proof Score side by side, and a
per-candidate evidence breakdown. This stub just proves the app runs.
"""

import streamlit as st

st.title("Resume Proof Scorer")
st.write("Scaffold running. Build the real UI here.")
